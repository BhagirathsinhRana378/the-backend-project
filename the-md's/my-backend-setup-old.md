till now we have just completed the setup of the backend files and other plugins to work properly with every thing in our backend and learn the backend

so in this file we will discuss all things about setting up my backend files and plugins
---
1st
we manded the pakage.json with npm init
---
2nd-
we maded src folder and then created db,controllers,middlewares,models,routs,utils files and stored theme in the src folder
---
3rd
we installed prettier and added some code to make our code prety when we formate the document
---
4th
we created the app.js, constants.js, index.js in the src folder
---
5th
we created the .env file and added the port number
---
6th
we build an account in mongoose atlas and created a data base and connected our data base with our project 
we wrote the mongodb url  in our env file
---
7th
we added the cookie-parsar package and cors from npm
imported them in the app.js
then--->
app.use(cors({
    origin: process.env.CORS_ORIGIN,
    // origin: 'http://example.com', // replace with your client's origin in production
    Credentials: true
    // why Credentials is set to true here?
    // Setting Credentials to true allows the server to accept cookies and authentication headers from the client, which is essential for maintaining user sessions and secure communication between the client and server.
}))
// why we are using .use(cors()) here?
// We are using app.use(cors()) to enable Cross-Origin Resource Sharing (CORS) for our Express application. This allows our server to accept requests from different origins, which is essential for web applications that interact with APIs hosted on different domains.
--+--
7.0th
 we updated the app.js and added 
 
app.use(cookieParser())//1
app.use(express.json({limit: '16kb'}))//2
app.use(express.urlencoded({ extended: true,limit: '16kb' }))//2
app.use(express.static('public'))//3

// 1 why are we using cookieParser() here?
// We are using cookieParser() middleware to parse cookies attached to the client request object. This allows us to easily access and manipulate cookies, which are often used for session management, user authentication, and storing user preferences.

// 2 why are we using express.json() and express.urlencoded() here?
// We are using express.json() and express.urlencoded() middleware to parse incoming request bodies in JSON format and URL-encoded format, respectively. This allows us to easily access and manipulate the data sent by clients in POST or PUT requests.

// 3 why are we using express.static('public') here?
// We are using express.static('public') to serve static files such as HTML, CSS, JavaScript, and images from the 'public' directory. This allows clients to access these files directly via HTTP requests. 
---
8th 
now we did make a utils called asyncHandler.js and apiError.js
and apiResponce.js

8.1.0-->asyncHandler.js
-->we first added a higher order fuinction
It helps us handle errors automatically without writing try-catch in every route
a function which can accpet parameters and can return them are called higher order functions

8.2.0-->apiError.js
--> We are creating a new class (a blueprint) called ApiError This class is meant to represent an error that comes from an API (backend server)
here we created a constructor
--The constructor is a special function that runs automatically

8.3.0-->apiResponce.js
--> we created a api responce class / constructor to respond our api's
class apiResponce {
    constructor(success, message, data = null) {

        this.statusCode = statusCode
        this.success = statusCode < 400
        this.message = message
        this.data = data

    }
}
---
9th
--> we created the files in the models folder
-1->user.model.js
-->
firstly we defiend the user schema modle 
then we added jwt tokens function to manage the clinet access to our project 
we added the below code:
    {
        // jwt is a birorary token that is used to authenticate users
// it is a string that is generated by the server and sent to the client
// the client then sends the token back to the server with each request
// the server then verifies the token and grants access to the requested resource

userSchema.methods.generateJwtToken = function () {
    return jwt.sign({
        _id: this._id,
        username: this.username,
        email: this.email,
        fullname: this.fullname
    }, 
    process.env.ACCESS_TOKEN_SECRET, { 
        expiresIn: process.env.ACCESS_TOKEN_EXPIRES_IN 
    }
)
}
that was for the generatejwttokens then we added for the jwt refresh tokens
:{
    userSchema.methods.generateRefreshToken = function () { 

    return jwt.sign({
        _id: this._id,
       
    }, 
    process.env.REFRESH_TOKEN_SECRET, { 
        expiresIn: process.env.REFRESH_TOKEN_EXPIRES_IN 
    }
)
here i dont know what we do with this one
chatgpt reply it propery for my readme and make the jwt tokens code and description biggner friendly and easy to understand and easy to remember and under at very most fundamental level
}
}
--

    
-2->video.model.js
-->as same as user modle we created the video model and added schema for the video model and also installed the mongooseAggregatePaginate
---

10th
we created our cloudinary account and linked it with our .env
we created a file in utils folder named cloudinary.js
here we config the cloudinary and wrote the code to upload our photo, video to the clouinary server and get url from it and also wrote the code to delet the photo and video form the server


then we added a file named multer.middleware.js in the middleware folder
and added the code:{

    import multer from "multer";

const storage = multer.diskStorage({
    destination: function (req, file, cb) {
        cb(null, "./public/temp")
    },
    filename: function (req, file, cb) {
       
        cb(null, file.originalname)
    }
})

export const upload = multer({ storage,})
}
// we saved the photo, video in the diskstorage
we gaved the destination into public/temp to save file
we added the a code that the file name will be the original filename uploaded by the user